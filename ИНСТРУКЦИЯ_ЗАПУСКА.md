# Инструкция по запуску KinoSwipe

## Что было добавлено:

### 1. Экран ожидания в комнате
- При создании или присоединении к комнате пользователи попадают на экран ожидания
- Показывается список участников комнаты
- Хост комнаты видит кнопку "🎬 Начать выбор фильмов" (активна при наличии минимум 2 участников)
- Остальные участники видят сообщение "⏳ Ожидание, пока хост начнет выбор фильмов..."
- Автоматическое обновление списка участников каждые 2 секунды
- Автоматический переход в режим свайпов, когда хост запускает комнату

### 2. API для управления комнатами
- `POST /api/v1/rooms/{room_id}/start` - запуск комнаты (только для хоста)
- `GET /api/v1/rooms/{room_id}/members` - получение списка участников комнаты

### 3. Настройка PostgreSQL для внешнего доступа
- Добавлены конфигурационные файлы `postgresql.conf` и `pg_hba.conf`
- PostgreSQL теперь принимает подключения извне на порту 5432
- Данные для подключения:
  - Host: localhost (или IP сервера)
  - Port: 5432
  - User: kinoswipe
  - Password: kinoswipe123
  - Database: kinoswipe

### 4. Библиотека фильмов и админ-панель
- Библиотека фильмов доступна через кнопку "🎬 Библиотека фильмов" на главной странице
- Админ-панель доступна после входа как администратор
- В админ-панели можно добавлять фильмы с полной информацией

## Запуск приложения:

### Вариант 1: Docker Compose (рекомендуется)
```bash
docker-compose up -d
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8080
- PostgreSQL: localhost:5432

### Вариант 2: Локальный запуск
1. Запустить PostgreSQL:
```bash
docker-compose up -d postgres
```

2. Запустить миграции:
```bash
cd /path/to/kinoswipe
migrate -path ./migrations -database "postgres://kinoswipe:kinoswipe123@localhost:5432/kinoswipe?sslmode=disable" up
```

3. Запустить backend:
```bash
cd /path/to/kinoswipe
go run cmd/server/main.go
```

4. Запустить frontend:
```bash
cd frontend
npm install
npm start
```

## Использование:

1. **Создание комнаты:**
   - Войдите в систему (быстрый вход или регистрация)
   - Пройдите опросник по жанрам (если первый раз)
   - Нажмите "➕ Создать комнату"
   - Вы попадете на экран ожидания

2. **Присоединение к комнате:**
   - Введите код комнаты или выберите из списка активных комнат
   - Вы попадете на экран ожидания

3. **Запуск выбора фильмов:**
   - Хост комнаты нажимает "🎬 Начать выбор фильмов"
   - Все участники автоматически переходят в режим свайпов
   - Алгоритм подбирает фильмы на основе предпочтений всех участников

4. **Добавление фильмов (админ):**
   - Войдите как администратор
   - Перейдите в раздел "Фильмы"
   - Заполните форму и нажмите "✅ Создать фильм"

## Подключение к PostgreSQL извне:

Для подключения из другого приложения или инструмента:
```
Host: localhost (или IP вашего сервера)
Port: 5432
User: kinoswipe
Password: kinoswipe123
Database: kinoswipe
```

## Важные замечания:

- Минимум 2 участника требуется для запуска комнаты
- Хост может запустить комнату только один раз
- После запуска комната переходит в статус "active"
- Все участники видят одинаковые фильмы для свайпов
- Матчи создаются автоматически, когда все участники лайкают один фильм
