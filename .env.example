# Подключение к PostgreSQL (только для локального запуска).
# Скопируйте в .env и подставьте свои данные: cp .env.example .env
#
# ⚠️ НА RAILWAY: этот файл НЕ подставляйте в Variables!
# Там нужно: Variables → + New Variable → Add Reference → PostgreSQL → DATABASE_URL.
# Подробно: см. RAILWAY_НАСТРОЙКА_БД.md

# Локально (для Railway не использовать):
# DATABASE_URL=postgres://user:password@localhost:5432/kinoswipe?sslmode=disable

# Вариант 2: отдельные переменные (если не задан DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=kinoswipe
# DB_PASSWORD=kinoswipe123
# DB_NAME=kinoswipe
# DB_SSLMODE=disable

# Сервер приложения (опционально)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8080
