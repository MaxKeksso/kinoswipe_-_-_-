# Финальные команды для применения всех изменений

## ✅ Что уже сделано:

1. ✅ Миграция применена (таблицы `premieres` и `match_links` созданы)
2. ✅ Админ создан (`admin@kinoswipe.ru` / `admin123`)
3. ✅ Backend handlers созданы:
   - `handlers/auth_handler.go` - авторизация и регистрация
   - `handlers/premiere_handler.go` - управление премьерами
   - `handlers/match_link_handler.go` - управление ссылками
4. ✅ Repositories созданы:
   - `repository/premiere_repository.go`
   - `repository/match_link_repository.go`
5. ✅ Frontend компоненты созданы и интегрированы
6. ✅ Роуты добавлены в `cmd/server/main.go`

## 🚀 Команды для применения:

### 1. Установить зависимость для bcrypt:

```bash
cd /Users/maksimzagorodnev/Downloads/kinoswipe
go get golang.org/x/crypto/bcrypt
go mod tidy
```

### 2. Пересобрать и перезапустить backend:

```bash
docker-compose build app
docker-compose up -d app
```

### 3. Проверить логи backend:

```bash
docker-compose logs app -f
```

Должны увидеть: `Server starting on 0.0.0.0:8080`

### 4. Пересобрать frontend (если еще не сделано):

```bash
docker-compose build frontend
docker-compose up -d frontend
```

### 5. Проверить статус всех сервисов:

```bash
docker-compose ps
```

Все должны быть "Up".

---

## ✅ Проверка работы:

### 1. Проверить API endpoints:

```bash
# Проверить health
curl http://localhost:8080/api/v1/health

# Получить премьеры
curl http://localhost:8080/api/v1/premieres

# Попробовать регистрацию
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@test.com","password":"test123"}'

# Попробовать вход админа
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@kinoswipe.ru","password":"admin123"}'
```

### 2. Открыть браузер:

```
http://localhost:3000
```

### 3. Очистить кеш браузера:

- Mac: `Cmd + Shift + R`
- Windows: `Ctrl + Shift + R`

---

## 📝 Использование:

### Для обычных пользователей:
1. Откройте `http://localhost:3000`
2. Вкладка "Регистрация"
3. Заполните форму (имя, email, пароль)
4. После регистрации - главная страница

### Для админов:
1. Откройте `http://localhost:3000`
2. Вкладка "Админ"
3. Email: `admin@kinoswipe.ru`
4. Пароль: `admin123`
5. Админ-панель → раздел "Премьеры" → добавление премьер

### После матча:
1. При матче появляется модальное окно
2. Нажмите "Где посмотреть?"
3. Откроется страница со ссылками
4. Клик по ссылке открывает платформу

---

## 🔍 Если что-то не работает:

### Проверить логи:

```bash
# Backend
docker-compose logs app -f

# Frontend
docker-compose logs frontend -f

# База данных
docker-compose logs postgres -f
```

### Перезапустить все:

```bash
docker-compose down
docker-compose build
docker-compose up -d
```

### Проверить компиляцию Go:

```bash
docker-compose exec app go build -o /tmp/test ./cmd/server/main.go
```

---

## ⚠️ Важно:

1. **Зависимость bcrypt** должна быть установлена перед сборкой backend
2. **Миграция** уже применена (таблицы созданы)
3. **Админ** уже создан
4. **Все handlers** созданы и интегрированы

---

**Готово! Все должно работать! 🎉**
