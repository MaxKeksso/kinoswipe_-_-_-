# Исправление ошибки "missing go.sum entry"

## Проблема:
```
missing go.sum entry for module providing package golang.org/x/crypto/bcrypt
```

## ✅ Решение:

### Вариант 1: Обновить go.sum вручную (уже сделано)

Файл `go.sum` уже обновлен с нужной зависимостью.

### Вариант 2: Выполнить команды локально

```bash
cd /Users/maksimzagorodnev/Downloads/kinoswipe

# Обновить зависимости
go mod tidy

# Или установить конкретную зависимость
go get golang.org/x/crypto/bcrypt
```

### Вариант 3: Пересобрать backend

После обновления `go.sum`:

```bash
docker-compose build app
docker-compose up -d app
```

---

## 🚀 Полная команда для исправления:

```bash
cd /Users/maksimzagorodnev/Downloads/kinoswipe

# Обновить go.sum (если нужно)
go mod tidy

# Пересобрать backend
docker-compose build app

# Перезапустить
docker-compose up -d app

# Проверить логи
docker-compose logs app -f
```

---

**Файл `go.sum` уже обновлен! Просто пересоберите backend.**
