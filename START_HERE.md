# 🚀 Начните отсюда!

## Быстрый запуск проекта (без установки Go и golang-migrate)

Поскольку у вас не установлены Go и golang-migrate локально, используйте Docker Compose - это самый простой способ запустить проект!

### Шаг 1: Запустите проект через Docker Compose

```bash
docker-compose up -d
```

Эта команда:
- ✅ Запустит PostgreSQL базу данных
- ✅ Применит миграции автоматически
- ✅ Запустит Go приложение

### Шаг 2: Проверьте, что всё работает

```bash
# Проверьте статус контейнеров
docker-compose ps

# Проверьте логи
docker-compose logs app

# Проверьте health endpoint
curl http://localhost:8080/api/v1/health
```

Должен вернуться: `OK`

### Шаг 3: Готово! 🎉

Приложение доступно по адресу: `http://localhost:8080`

---

## Альтернативные способы запуска

### Если хотите использовать миграции отдельно:

```bash
# Запустите только базу данных
docker-compose up -d postgres

# Примените миграции через Docker
./scripts/run_migrations_docker.sh
```

### Если позже установите Go локально:

1. **Установите Go**: https://golang.org/dl/
2. **Установите golang-migrate**: `brew install golang-migrate`
3. Запустите миграции: `./scripts/init_db.sh`
4. Запустите приложение: `go run cmd/server/main.go`

---

## Полезные команды

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить данные БД
docker-compose down -v

# Посмотреть логи
docker-compose logs -f app

# Перезапустить приложение
docker-compose restart app

# Войти в контейнер PostgreSQL
docker-compose exec postgres psql -U kinoswipe -d kinoswipe
```

---

## Что дальше?

1. 📖 Прочитайте `QUICKSTART.md` для примеров использования API
2. 📚 Изучите `README.md` для полной документации
3. 🏗️ Посмотрите `ARCHITECTURE.md` для понимания структуры проекта

---

## Устранение проблем

### Порт 8080 занят?
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "3000:8080"  # Вместо 8080:8080
```

### Порт 5432 занят?
Измените порт PostgreSQL:
```yaml
ports:
  - "5433:5432"  # Вместо 5432:5432
```

### Ошибки при сборке?
```bash
# Пересоберите образ
docker-compose build --no-cache app
```

---

**Всё готово к работе! Начните с `docker-compose up -d` 🎬**



